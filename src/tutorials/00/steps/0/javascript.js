// Hit Shift-Enter to execute this code
var ractive = new Ractive({
  el: output,
  template: template
});

// Throughout this tutorial, the `output` variable refers
// to the box in the bottom left. The `template` variable
// refers to the contents of the box above this one.
