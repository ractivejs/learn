{"copy":"<p>Having two properties (<code>signedIn</code> and <code>notSignedIn</code>) to represent one piece of data (whether or not the user is signed in) is the sort of thing that makes most programmers itch uncontrollably.</p>\n\n<p>We have a couple of options. We could use an <code>#unless</code> section:</p>\n\n<pre class='prettyprint lang-html'>\n{{#unless signedIn}}\n  &lt;!-- not-signed-in block --&gt;\n{{/unless}}\n</pre>\n\n<p>Alternatively, we could use <code>else</code>:</p>\n\n<pre class='prettyprint lang-html'>\n{{#if signedIn}}\n  &lt;!-- signed-in block --&gt;\n{{else}}\n  &lt;!-- not-signed-in block --&gt;\n{{/if}}\n</pre>\n\n<p>Update the template. Then, remove the references to <code>notSignedIn</code> in the JavaScript. Then breathe a sigh of relief &ndash; doesn't that feel better?</p>\n","init":true,"javascript":"var ractive = new Ractive({\n  el: output,\n  template: template,\n  data: {\n    signedIn: false,\n    notSignedIn: true\n  }\n});\n\nractive.on( 'signIn', function () {\n  var name = prompt( 'Enter your username to sign in', 'ractive_fan' );\n\n  ractive.set({\n    username: name,\n    signedIn: true,\n    notSignedIn: false\n  });\n});","template":"{{#if signedIn}}\n  <!-- message for signed-in users -->\n  <p>Welcome back, {{username}}!</p>\n{{/if}}\n\n{{#if notSignedIn}}\n  <!-- message for non-signed-in users -->\n  <p>Hi there! Please <a class='button' on-tap='signIn'>sign in</a></p>\n{{/if}}\n","fixed":{"javascript":"var ractive = new Ractive({\n  el: output,\n  template: template,\n  data: {\n    signedIn: false\n  }\n});\n\nractive.on( 'signIn', function () {\n  var name = prompt( 'Enter your username to sign in', 'ractive_fan' );\n\n  ractive.set({\n    username: name,\n    signedIn: true\n  });\n});","template":"{{#if signedIn}}\n  <!-- message for signed-in users -->\n  <p>Welcome back, {{username}}!</p>\n{{else}}\n  <!-- message for non-signed-in users -->\n  <p>Hi there! Please <a class='button' on-tap='signIn'>sign in</a></p>\n{{/if}}\n"},"index":1,"numSiblings":2,"tutorialTitle":"Conditional sections","tutorialIndex":4}